<div class="col-md-10 col-md-offset-1 col-xs-10 col-xs-offset-1">
  <ul class="nav nav-tabs">
    <li class="active"><a href="#ready-for-delivery-orders" data-toggle="tab" aria-expanded="true">Ready for Delivery Orders</a></li>
    <li class=""><a href="#delivered-orders" data-toggle="tab" aria-expanded="false">Delivered Orders</a></li>
    <li class=""><a href="#cancelled-orders" data-toggle="tab" aria-expanded="false">Cancelled Orders</a></li>
  </ul>

  <div id="myTabContent" class="tab-content">
    <div class="tab-pane fade active in" id="ready-for-delivery-orders">
      <%= render partial: 'order', collection: @ready_for_delivery_orders %>
      <% if @ready_for_delivery_orders.empty? %>
        <h3>No Orders for delivery</h3>
      <% end %>
    </div>

    <div class="tab-pane fade" id="delivered-orders">
      <%= render partial: 'order', collection: @delivered_orders %>
      <% if @delivered_orders.empty? %>
        <h3>No Delivered Orders</h3>
      <% end %>
    </div>
    
    <div class="tab-pane fade" id="cancelled-orders">
      <%= render partial: 'order', collection: @cancelled_orders %>
      <% if @cancelled_orders.empty? %>
        <h3>No Cancelled Orders</h3>
      <% end %>
    </div>
  </div>
</div>
