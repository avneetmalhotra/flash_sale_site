<nav class="navbar navbar-default">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to 'FLASH SALE', root_url, class: 'navbar-brand' %>
    </div>

    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <% if current_user.present? %>
        <ul class="nav navbar-nav">
          <li><%= link_to t(:deals, scope: [:action]), root_url %></li>
        </ul>

        <ul class="nav navbar-nav navbar-right">
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false">
              <%= image_tag 'user_profile_logo.png', size: '25x25' %>
            <span class="caret"></span></a>
            <ul class="dropdown-menu" role="menu">
              <li><%= link_to t(:update_profile, scope: [:action]), edit_registration_url(current_user), method: :get %></li>
              <li class="divider"></li>
              <li><%= link_to 'Logout', logout_url, method: :delete %></li>
            </ul>
          </li>
        </ul>

      <% else %>

        <ul class="nav navbar-nav navbar-right">
          <li><%= link_to t(:login, scope: [:action]).upcase, login_url %></li>
        </ul>

      <% end %>
    </div>
  </div>
</nav>
