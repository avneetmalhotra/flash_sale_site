<% if flash.any? %>
  <div class="flash-display alert alert-dismissible alert-warning">
    <button type="button" class="close" data-dismiss="alert">&times;</button>
    <% flash.each do |name, msg| %>
      <%= content_tag :p, msg, class: 'mb-0' %>
    <% end %>
  </div>
<% end %>