<%= render 'form', user: @user, url: registration_path(@user), method: :patch, form_legend: 'Update Profile', email_disable_status: true do |form| %>
    <%= form.submit 'Update Profile', class: 'btn btn-primary col-md-offset-5' %>
<% end %>